<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- use your Material surface color -->
    <solid android:color="?attr/colorSurface" />
    <!-- match your theme’s default component corner size -->
    <corners android:radius="16dp" />
    <!-- optional padding inside the dialog -->
    <padding
        android:left="24dp"
        android:top="24dp"
        android:right="24dp"
        android:bottom="24dp"/>
</shape>
